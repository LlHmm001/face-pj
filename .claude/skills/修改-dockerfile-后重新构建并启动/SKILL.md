---
name: 修改 Dockerfile 后重新构建并启动
description: 我刚修改了 Dockerfile，请帮我重新构建并启动项目。
---

我刚修改了 Dockerfile，请帮我重新构建并启动项目。

要求：
1. 使用 docker compose 重新 build。
2. 必要时强制重建容器。
3. 启动所有服务。
4. 检查容器状态和日志。
5. 如果构建失败，请根据错误修复 Dockerfile。